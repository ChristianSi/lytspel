Currently, this is just a dummy test:

* Open `input.html` in your browser with the extension loaded and store the
  result as `output.html`.
* Run `make` to check that the result is as expected (no output) --
  otherwise, the differences will be shown.
